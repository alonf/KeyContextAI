# Review Result

- **Campaign**: `cmp-001-layout-autocorrect-i002`
- **Run**: `run-20260827-122146212-0e6d630d`
- **Harness**: `codex-cli-file-primary`
- **Target digest**: `b3ce35ea8430e0f49bb0d46f2ca7cfdb885c9f63`
- **Completion**: `complete`
- **Verdict**: `findings`
- **Runtime outcome**: `completed`
- **Currentness**: `current`
- **Can approve current snapshot**: `false`

## Summary

Completed a risk-based review of the iteration-002 native input, focus/privacy, injection, layout, lifecycle, and focused test surfaces. The controller's declared governance command succeeded for the target digest, but the implementation still has one blocking privacy/order defect and several major input-integrity and resilience defects.

## Findings

| ID | Severity | Relevance | Location | Finding |
| --- | --- | --- | --- | --- |
| `finding-bc8166e198b049b9` | minor | current | src/KeyContextAI.Platform/Input/KeystrokeAccessor.cs:221 | Queued keystrokes lose their originating focus and password context: [demoted to minor: no concrete failure scenario, so it cannot gate; reported as blocking by the reviewer] The hook queues only virtual key, scan code, and time. The consumer later samples the then-current foreground thread/layout, while CorrectionManager gates the event using the most recently delivered FocusContext from a separate WinEvent callback stream. Those streams have no shared sequence or origin identity. A key queued in a password field can therefore be consumed after focus resolves to a normal field and be retained, and a normal key can be translated under a later window's layout. This violates FR-003 and the KeyEvent contract that LayoutId is the layout active when the key was pressed. Carry an immutable focus/epoch and layout snapshot with each queued event and reject it unless it matches the current known-safe context before any typed data is buffered or published. |
| `finding-06c2840e02891b70` | minor | current | src/KeyContextAI.Platform/System/FocusAccessor.cs:37 | Focus changes into the application bypass the privacy wipe: [demoted to minor: no concrete failure scenario, so it cannot gate; reported as major by the reviewer] Both focus hooks use WINEVENT_SKIPOWNPROCESS. When focus moves from another application into a KeyContextAI-owned window, FocusAccessor does not publish that boundary, so CorrectionManager can retain the prior transcript and continue treating the old PasswordState.No context as current. This contradicts FR-003's wipe-on-every-focus-change rule and becomes especially unsafe when settings or credential controls are added. Observe own-process focus changes too, or explicitly publish a fail-closed boundary on application activation. |
| `finding-09b4224decfd64a3` | minor | current | src/KeyContextAI.Platform/Input/KeystrokeAccessor.cs:225 | Armed suppression is not correlated with its token or committing key: [demoted to minor: no concrete failure scenario, so it cannot gate; reported as major by the reviewer] Arm stores a SuppressionToken, but the callback never reads or matches it. Any later Separator or Committing key clears the flag and is swallowed, even though the interface promises suppression of the next committing key. An intervening character can make the speculative verdict stale before the consumer disarms it, and Space or punctuation can then be suppressed with no token or suppression marker in the queued KeyEvent for reliable correlation. Bind the armed generation to the exact eligible event and propagate the consumed token/suppression state to the off-thread transaction. |
| `finding-dd4135780021ccf8` | minor | current | src/KeyContextAI.Platform/Input/InputInjectionAccessor.cs:133 | Odd partial SendInput results are compensated with the wrong text state: [demoted to minor: no concrete failure scenario, so it cannot gate; reported as major by the reviewer] The partial-failure accounting divides the inserted event count by two and assumes a trailing lone keydown did not change the document. Backspace keydown can delete a character and Unicode keydown can insert one, so an odd SendInput prefix can already have mutated text. The computed AppliedBackspaceCount/AppliedReplacementText is then wrong and compensation can leave text deleted, duplicated, or with a key logically down. Account for each emitted step's effect, including an odd trailing event, and cover those prefixes with a controllable SendInput seam. |
| `finding-7bd31dbce1030947` | minor | current | src/KeyContextAI.Core/Engines/WordAssemblyEngine.cs:27 | The promised 256-character in-memory bound is not enforced: [demoted to minor: no concrete failure scenario, so it cannot gate; reported as major by the reviewer] WordAssemblyEngine appends indefinitely to both StringBuilder and the scan-code list until a boundary arrives, and it copies both collections on every keystroke. CorrectionManager limits only the number of completed entries; each entry and the current word remain unbounded. A long boundary-free input can therefore retain arbitrary typed text and cause quadratic allocation, contrary to the hardening gate's explicit maximum of 256 characters per focus session. Enforce the character bound and reset/fail closed on overflow. |
| `finding-0ad24bcd74550071` | minor | current | src/KeyContextAI.Platform/Input/KeystrokeAccessor.cs:53 | Channel overflow silently corrupts the on-screen text model: [demoted to minor: no concrete failure scenario, so it cannot gate; reported as major by the reviewer] The hook channel uses DropOldest and ignores TryWrite's outcome. If the consumer stalls, arbitrary earlier keystrokes disappear while later ones remain, so word assembly no longer describes the text that reached the application. A later correction can consequently backspace or replace the wrong span. Overflow should invalidate/reset the current epoch (or publish an explicit gap) rather than continue from a silently truncated sequence. |
| `finding-c8b920cf53986f94` | minor | current | src/KeyContextAI.Platform/Input/KeystrokeAccessor.cs:324 | Common shifted punctuation is classified as word content: [demoted to minor: no concrete failure scenario, so it cannot gate; reported as major by the reviewer] Key classification is based only on virtual-key ranges. Shifted digit keys such as !, @, #, $, %, ^, &, *, (, and ) retain VK_0 through VK_9 and therefore fall through as Character rather than Separator. WordAssemblyEngine appends the punctuation and does not complete the word at the required punctuation boundary, causing missed or incorrect correction spans. Classify boundaries from the translated character (while preserving digit characters) and add shifted-punctuation coverage. |

_This Markdown is a controller-generated projection. Authority is the sibling immutable `result.json`._
