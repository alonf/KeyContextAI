# Review Result

- **Campaign**: `cmp-001-layout-autocorrect-i002`
- **Run**: `run-20260827-130507200-ca1ad260`
- **Harness**: `codex-cli-file-primary`
- **Target digest**: `73f97b38742502c114eab48bb4d07eb233dbe848`
- **Completion**: `complete`
- **Verdict**: `findings`
- **Runtime outcome**: `completed`
- **Currentness**: `snapshot-moved`
- **Can approve current snapshot**: `false`

## Summary

Risk-based review completed over the iteration-002 native input, focus/privacy, injection, layout, manager lifecycle, and associated tests. The controller's sole governance command passed for the exact digest, but it supplied no runtime-test evidence. Two approval-blocking boundary races and seven additional defects were established in the frozen code.

Failure reason: snapshot-moved

## Findings

| ID | Severity | Relevance | Location | Finding |
| --- | --- | --- | --- | --- |
| `finding-e4574aca4abd43c6` | minor | snapshot-moved | src/KeyContextAI.Core/Managers/CorrectionManager.cs:177 | Password keystrokes can beat the fail-closed focus event within one window: [demoted to minor: no concrete failure scenario, so it cannot gate; reported as blocking by the reviewer] Keystrokes and focus notifications arrive on separate callback streams, but the origin gate compares only the source window handle. For controls that share one top-level HWND, a key pressed just after focus moves from an ordinary control to a password control can be consumed before FocusAccessor publishes its provisional Unknown state. The manager still holds PasswordState.No and the same window identity, so it appends the password character. This violates FR-003 and User Story 5. Carry and validate focused-control identity or establish an ordering mechanism that makes the focus boundary visible before any key from the new control can be retained. |
| `finding-180a76094b284c33` | minor | snapshot-moved | src/KeyContextAI.Platform/Input/InputInjectionAccessor.cs:36 | Correction injection ignores the transaction target window: [demoted to minor: no concrete failure scenario, so it cannot gate; reported as blocking by the reviewer] ApplyCorrectionAsync receives a CorrectionTransaction with TargetWindowHandle, but never reads that handle before SendInput. SendInput delivers to whichever window is foreground at call time, so a focus change after the manager's applicability check and before the burst sends backspaces and replacement text into the new window. Compensation and suppressed-key reinjection have the same problem. This defeats the claimed FR-012 focus-change abandon rule and can destructively edit an unrelated application. Revalidate the native foreground target at the injection boundary and abandon before the first mutating event when it differs. |
| `finding-d3ad52a9f700d18c` | minor | snapshot-moved | src/KeyContextAI.Platform/System/FocusAccessor.cs:133 | Focus and keystroke accessors use incompatible window identities: [demoted to minor: no concrete failure scenario, so it cannot gate; reported as major by the reviewer] For EVENT_OBJECT_FOCUS, HandleWinEvent stores the event hwnd directly as FocusContext.WindowHandle. That handle commonly belongs to the focused child edit control, while HookCallback records GetForegroundWindow(), which is the top-level window. CorrectionManager then rejects every key because the handles differ, so ordinary typing never accumulates in common HWND-based controls. Normalize both streams to the same top-level identity while retaining a separate focused-control identity for the password gate. |
| `finding-a2ddb079893409cc` | minor | snapshot-moved | src/KeyContextAI.Platform/Input/KeystrokeAccessor.cs:344 | Deferred character translation reads keyboard state from the wrong thread: [demoted to minor: no concrete failure scenario, so it cannot gate; reported as major by the reviewer] PendingKey snapshots the virtual key, scan code, source window, and layout, but not modifier/toggle state. TryTranslateCharacter later calls GetKeyboardState on the dedicated consumer thread; Win32 keyboard state follows the calling thread's input/message queue, and this consumer does not process the target application's keyboard messages. Shift, Caps Lock, Ctrl/AltGr, and similar state can therefore be stale or absent. For example Shift+1 can be translated as '1' instead of '!', preventing the punctuation boundary and making the transcript diverge from displayed text. Snapshot the relevant state at capture time or translate with a correctly synchronized input queue. |
| `finding-d111379e93b7f66c` | minor | snapshot-moved | src/KeyContextAI.Core/Managers/CorrectionManager.cs:72 | A pre-pause correction becomes applicable again after resume: [demoted to minor: no concrete failure scenario, so it cannot gate; reported as major by the reviewer] Pause and Stop wipe the transcript but do not advance the epoch. IsCorrectionApplicable rejects a transaction while paused, but after Resume in the same safe window the paused flag clears and the old transaction's entries still match the unchanged epoch, so the stale transaction becomes applicable again. A delayed correction can therefore edit text retained before a lifecycle boundary that FR-003 requires to discard. Advance the epoch on every pause/stop lifecycle invalidation, not only on focus changes and sequence gaps. |
| `finding-18def3c85812f38d` | minor | snapshot-moved | src/KeyContextAI.Platform/Input/KeystrokeAccessor.cs:236 | An armed suppression can consume a later unrelated boundary key: [demoted to minor: no concrete failure scenario, so it cannot gate; reported as major by the reviewer] Arm records only a token, and HookCallback consumes it on any future Separator or Committing key. Navigation keys, sequence gaps, and focus changes do not clear the armed state; CorrectionManager disarms only on pause, stop, or disposal. If the user moves the caret or changes windows after arming, the next Space, Tab, punctuation key, or Enter in the new context is suppressed. Bind suppression to the expected key/context and disarm on every transcript-invalidating event so user input cannot be withheld after its transaction is stale. |
| `finding-54ed10e5da0e89f4` | minor | snapshot-moved | src/KeyContextAI.Platform/Input/InputInjectionAccessor.cs:257 | Suppressed key reinjection does not preserve its modifier chord: [demoted to minor: no concrete failure scenario, so it cannot gate; reported as major by the reviewer] KeyEvent carries no modifier snapshot and ReinjectKeyAsync emits only the suppressed key's down/up pair. If Shift+Enter, Ctrl+Enter, or another chord is suppressed and the user releases the modifier while correction runs, reinjection delivers plain Enter instead. In chat applications this can turn a requested newline or alternate submit action into an unintended send. Capture the original modifier context and replay the original gesture semantics, or do not arm modified committing keys. |
| `finding-02834c21354853b0` | minor | snapshot-moved | src/KeyContextAI.Core/Managers/CorrectionManager.cs:69 | Capture remains suspended until an external focus event happens: [demoted to minor: no concrete failure scenario, so it cannot gate; reported as major by the reviewer] CorrectionManager starts with PasswordState.Unknown and no current focus. StartAsync only installs the keyboard hook, while FocusAccessor publishes contexts only in response to later WinEvent callbacks and exposes no current FocusContext snapshot. If the application starts while the user keeps typing in the already-focused control, every key is discarded indefinitely until focus changes. Initialize and publish a fail-closed current-focus snapshot as part of startup before declaring the hook ready. |
| `finding-cb2f969d3a97d941` | minor | snapshot-moved | tests/KeyContextAI.Core.Tests/CorrectionManagerPrivacyTests.cs:44 | The pause privacy test passes without exercising transcript wiping: Pause_WipesTranscriptAndSuspendsCapture never publishes a PasswordState.No focus context. The manager therefore stays in its initial Unknown state and rejects the two test keystrokes, leaving TranscriptCount at zero before Pause is called. Removing WipeTranscript from Pause would not fail this test, so the claimed pause-boundary privacy control is not protected. Establish a safe focus, assert a nonzero transcript before Pause, then assert it is erased. |

_This Markdown is a controller-generated projection. Authority is the sibling immutable `result.json`._
