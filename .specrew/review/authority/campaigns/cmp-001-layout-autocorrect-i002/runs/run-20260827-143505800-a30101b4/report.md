# Review Result

- **Campaign**: `cmp-001-layout-autocorrect-i002`
- **Run**: `run-20260827-143505800-a30101b4`
- **Harness**: `codex-cli-file-primary`
- **Target digest**: `b5724e07d933e3f2607b64c9928df02ecd9d0986`
- **Completion**: `complete`
- **Verdict**: `findings`
- **Runtime outcome**: `completed`
- **Currentness**: `current`
- **Can approve current snapshot**: `false`

## Summary

Completed a risk-based review of the six implemented iteration-002 tasks, emphasizing the password fail-closed boundary, low-level hook latency and ordering, suppression recovery, injection integrity, layout identity, and their regression tests. The controller's configured governance command succeeded against the target digest, but inspection found two blocking privacy defects, six major input/correction defects, and one minor suppression defect.

## Findings

| ID | Severity | Relevance | Location | Finding |
| --- | --- | --- | --- | --- |
| `finding-76f6af1b0dafd488` | minor | current | src/KeyContextAI.Platform/System/FocusAccessor.cs:267 | Unsupported UI Automation password state is treated as safe: [demoted to minor: no concrete failure scenario, so it cannot gate; reported as blocking by the reviewer] The password probe reads current.IsPassword, whose UI Automation accessor supplies the property's default false value when the focused provider does not implement IsPassword. The code converts every such false to PasswordState.No, so an unsupported or incomplete provider is classified as explicitly safe and CorrectionManager retains its keystrokes. This contradicts the required fail-closed Unknown behavior and can capture password text from controls whose providers omit the property. Query IsPasswordProperty while ignoring defaults and map AutomationElement.NotSupported to Unknown. |
| `finding-db950d1c31c8e86f` | minor | current | src/KeyContextAI.Core/Managers/CorrectionManager.cs:212 | Logical focus changes can retain password keys under the previous safe context: [demoted to minor: no concrete failure scenario, so it cannot gate; reported as blocking by the reviewer] The origin gate distinguishes controls only when both streams provide different HWNDs. UI Automation fields commonly share one host HWND (or expose no control HWND), so moving from a normal field to a password field leaves SourceControlHandle equal to the old handle or zero. Because focus events and hook events are independent streams, a key can then reach HandleKeyObserved before the provisional Unknown focus event and pass under the previous PasswordState.No. The code's own correlation model has no UIA element identity or sequence that can reject this key, violating the requirement that password-field input is never captured. |
| `finding-a05fddf5ead8b1a5` | minor | current | src/KeyContextAI.Platform/Input/KeystrokeAccessor.cs:242 | Keyboard state is sampled from the hook thread instead of the typing thread: [demoted to minor: no concrete failure scenario, so it cannot gate; reported as major by the reviewer] GetKeyboardState reports the calling thread's queue state. HookCallback runs on the accessor's private message-pump thread, which is not attached to the foreground application's input thread, so the captured Shift/Ctrl/Alt/toggle state does not reliably describe the user's key. ToUnicodeEx can therefore render the wrong character and classify shifted punctuation incorrectly, while deferred replay can turn Shift+Enter into bare Enter and submit a message. Capture modifiers from a source that is valid for WH_KEYBOARD_LL or maintain an event-ordered modifier state rather than calling GetKeyboardState on this thread. |
| `finding-682d2fa5b0cbaab6` | minor | current | src/KeyContextAI.Platform/Input/KeystrokeAccessor.cs:224 | The low-level hook fast path allocates and performs multiple synchronous queries: [demoted to minor: no concrete failure scenario, so it cannot gate; reported as major by the reviewer] Each keydown performs foreground, ancestor, thread, layout, focused-control, and keyboard-state calls and allocates a new 256-byte array before it returns from the low-level hook. This is not the approved allocation-free atomic-flag-plus-enqueue path. GC or a slow synchronous query can exceed LowLevelHooksTimeout, after which Windows silently removes the hook and global capture stops. Move origin/state collection off the callback or use a preallocated, demonstrably bounded native snapshot path. |
| `finding-6f0cd4db04cd5c8c` | minor | current | src/KeyContextAI.Platform/Input/KeystrokeAccessor.cs:54 | Queue overflow can permanently swallow a suppressed key: [demoted to minor: no concrete failure scenario, so it cannot gate; reported as major by the reviewer] The pending channel uses DropOldest. When full, TryWrite succeeds after silently discarding the oldest PendingKey, so the explicit failure counter is not set. If the discarded item carries the suppression token, the original keydown was already withheld from the application and no consumer remains that can re-inject it. A later sequence-gap notification can wipe state but cannot recover the lost Enter, Tab, or separator. Suppressed events need a non-droppable delivery/recovery path, and every drop mode must surface the gap immediately. |
| `finding-6045058b01e04ba6` | minor | current | src/KeyContextAI.Core/Managers/CorrectionManager.cs:86 | Lifecycle invalidation races with suppression consumption: [demoted to minor: no concrete failure scenario, so it cannot gate; reported as major by the reviewer] Pause, stop, focus change, and sequence-gap handling invalidate manager state under _stateGate but call Disarm only after releasing that lock. The hook's armed flag is independent of the manager lock, so a boundary key can consume the stale token and be suppressed in the interval. HandleKeyObserved then drops it because the manager is paused, suspended, or on another focus, and nothing re-injects the user's key. The invalidation and suppression state transition need one atomic protocol or an explicit compensating delivery for a concurrently consumed token. |
| `finding-5498d4b7fa5b4bca` | minor | current | src/KeyContextAI.Platform/Input/InputInjectionAccessor.cs:74 | Suppressed-key replay failure leaves corrected text mutated and loses the key: [demoted to minor: no concrete failure scenario, so it cannot gate; reported as major by the reviewer] After a correction burst succeeds, ApplyCorrectionAsync re-injects the suppressed key separately and simply returns that failure. If focus changes in this gap or SendInput rejects the replay, the source document is already modified while Enter/Tab remains swallowed; no rollback or recoverable result contains the original text. On the partial-failure path, the method also attempts to replay the committing key even when compensation itself reports that the target text remains modified, which can submit corrupted content. Treat replacement, compensation, layout ordering, and key delivery as a transaction whose failed terminal states cannot silently combine mutation with input loss. |
| `finding-ccd15f0b483aba20` | minor | current | src/KeyContextAI.Platform/System/LayoutAccessor.cs:41 | Distinct installed keyboard layouts collapse to one locale identifier: [demoted to minor: no concrete failure scenario, so it cannot gate; reported as major by the reviewer] LayoutIdFromKeyboardLayout keeps only the HKL's low-word LANGID, and GetInstalledLayouts then de-duplicates those values. Distinct layouts for the same locale, such as US QWERTY and US Dvorak, both become en-US; a three-layout machine can be reported as having only two candidates, and TrySwitchLayout selects the first matching HKL rather than the intended one. This defeats FR-005a's compare-every-installed-layout rule and can translate or switch using the wrong physical map. Preserve a stable full-HKL/layout identity separately from the language tag used for dictionaries. |
| `finding-c461a0db389cceff` | minor | current | src/KeyContextAI.Platform/Input/KeystrokeAccessor.cs:213 | Suppression withholds keydown but lets the original keyup reach the application: HookCallback immediately passes every WM_KEYUP and WM_SYSKEYUP through, while an armed transaction suppresses only the matching keydown. The target can therefore receive an orphan keyup before the deferred injected down/up pair, or an extra keyup afterward. Applications and accessibility/hotkey handlers that act on release can observe or execute the supposedly withheld gesture out of order. Correlate and suppress the original release for a consumed token, then replay one complete gesture. |

_This Markdown is a controller-generated projection. Authority is the sibling immutable `result.json`._
