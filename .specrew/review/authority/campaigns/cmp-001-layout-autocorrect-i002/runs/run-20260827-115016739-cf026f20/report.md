# Review Result

- **Campaign**: `cmp-001-layout-autocorrect-i002`
- **Run**: `run-20260827-115016739-cf026f20`
- **Harness**: `codex-cli-file-primary`
- **Target digest**: `96236727428ed64ee9d9bb70038499f55bb89be7`
- **Completion**: `complete`
- **Verdict**: `findings`
- **Runtime outcome**: `completed`
- **Currentness**: `current`
- **Can approve current snapshot**: `false`

## Summary

Risk-based review covered the iteration design contracts, controller verification evidence, all implemented native input/focus/correction surfaces, their interfaces and models, and adjacent tests. The governance command succeeded, but the implementation has a privacy-blocking focus-transition race and major hook, suppression, key-classification, and partial-injection defects.

## Findings

| ID | Severity | Relevance | Location | Finding |
| --- | --- | --- | --- | --- |
| `finding-da66b09f8236202c` | minor | current | src/KeyContextAI.Platform/System/FocusAccessor.cs:125 | Password focus is not made fail-closed before UI Automation runs: [demoted to minor: no concrete failure scenario, so it cannot gate; reported as blocking by the reviewer] HandleWinEvent does not publish the new focus until TryBuildContext finishes synchronous window and UI Automation probes. If focus moves from an ordinary field while the automation provider stalls, CorrectionManager retains the previous PasswordState.No and the independent keyboard hook continues feeding characters into word assembly and the transcript. Text typed into the new password field can therefore be retained before Unknown or Yes is delivered. Publish an immediate boundary with Unknown (and wipe/disarm capture) when the native focus event arrives, then upgrade to No only after a successful bounded probe. |
| `finding-8398f66d6075a89f` | minor | current | src/KeyContextAI.Platform/Input/KeystrokeAccessor.cs:182 | The low-level hook executes the managed pipeline synchronously: [demoted to minor: no concrete failure scenario, so it cannot gate; reported as major by the reviewer] HookCallback constructs a KeyEvent and synchronously invokes every KeyObserved subscriber before returning to Windows. The current CorrectionManager subscriber takes _stateGate and calls WordAssemblyEngine, which creates strings and arrays on every key; Resume can also hold that same gate while calling UI Automation. This contradicts the approved allocation-free O(1) callback design and can delay typing or exceed LowLevelHooksTimeout, causing Windows to remove the hook. The callback should capture only minimal native data and enqueue it to an off-hook-thread consumer. |
| `finding-71b4996b33c31b50` | minor | current | src/KeyContextAI.Platform/Input/KeystrokeAccessor.cs:188 | Armed suppression swallows every key and never consumes its token: [demoted to minor: no concrete failure scenario, so it cannot gate; reported as major by the reviewer] Once _armed is nonzero, HookCallback returns 1 for every non-self-injected keydown, without checking that the key is committing and without atomically clearing the armed state. The stored SuppressionToken is never read. From Arm until some later Disarm, ordinary characters and multiple key presses can be lost, whereas the interface and Option B design require suppression of exactly the intended committing key. Match the eligible key and consume the arm state atomically in the callback. |
| `finding-619e657c16856fee` | minor | current | src/KeyContextAI.Platform/Input/KeystrokeAccessor.cs:245 | Native key classification breaks word boundaries and caret edits: [demoted to minor: no concrete failure scenario, so it cannot gate; reported as major by the reviewer] ClassifyKey marks Tab as Separator even though the model and FR-005b define Tab as a committing key; punctuation falls through as Character and is appended instead of completing the word; navigation/function keys also fall through as Character with no character, so WordAssemblyEngine does not reset stale text; and forward Delete is treated like Backspace, shortening the prior text even though it deletes ahead of the caret. These cases produce missed boundaries or correction spans that no longer match on-screen text. Classify committing, separator, backward-editing, and reset keys according to their actual semantics. |
| `finding-b5b9e5046dbe5863` | minor | current | src/KeyContextAI.Platform/Input/InputInjectionAccessor.cs:123 | A partial SendInput failure leaves user text mutated: [demoted to minor: no concrete failure scenario, so it cannot gate; reported as major by the reviewer] SendBurst treats a short SendInput count as failure after some prefix of backspaces or replacement events may already have been inserted. ApplyCorrectionAsync then only attempts to re-inject the suppressed key; it neither restores the original span nor reports which events were applied, and InjectionResult cannot carry that count. The documented hardening control says an injection failure leaves the original text intact, but this path can leave it partially deleted or replaced. Preserve enough transaction/result information to compensate a partial insertion before returning failure. |
| `finding-9c4f391a858631b5` | minor | current | src/KeyContextAI.Platform/System/FocusAccessor.cs:80 | Caret coordinates are returned in client rather than screen space: GetGUIThreadInfo supplies rcCaret relative to hwndCaret, but TryGetCaretPosition returns its midpoint directly. IFocusAccessor and FocusContext promise screen coordinates, so the future caret-anchored overlay will be offset by the target control or window origin. Convert the point through ClientToScreen using info.HwndCaret before returning it. |

_This Markdown is a controller-generated projection. Authority is the sibling immutable `result.json`._